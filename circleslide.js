$(".slide").children().css("width", "8em");

(function() {
    var method;
    var noop = function() {};
    var methods = [
      'Rei', '', 'Liz', 'Gaeul', 'Leeseo', 'An Yujin', 'ang Wonyoung',
      
    ];
    var length = methods.length;
    var console = (window.console = window.console || {});


})